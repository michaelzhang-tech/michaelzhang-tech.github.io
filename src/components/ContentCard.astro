---
interface Props {
  title: string;
  description: string;
  date: Date;
  href: string;
  tags?: string[];
  meta?: string;
}

const { title, description, date, href, tags = [], meta } = Astro.props;

const formattedDate = date.toLocaleDateString('zh-CN', {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
});
---

<article class="group bg-white rounded-xl border border-slate-200 overflow-hidden hover:shadow-lg transition-shadow">
  <a href={href} class="block p-6">
    <div class="flex items-center gap-2 text-sm text-slate-500 mb-3">
      <time>{formattedDate}</time>
      {meta && (
        <>
          <span>·</span>
          <span>{meta}</span>
        </>
      )}
    </div>
    
    <h2 class="text-xl font-bold text-slate-900 group-hover:text-blue-600 transition-colors mb-2">
      {title}
    </h2>
    
    <p class="text-slate-600 line-clamp-2 mb-4">
      {description}
    </p>
    
    {tags.length > 0 && (
      <div class="flex flex-wrap gap-2">
        {tags.map((tag) => (
          <span class="px-3 py-1 text-xs font-medium bg-slate-100 text-slate-600 rounded-full">
            {tag}
          </span>
        ))}
      </div>
    )}
  </a>
</article>